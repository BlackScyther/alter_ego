# Project metadata

| File | Purpose |
|------|---------|
| `editor.json` | Character editor wizard: steps, compendium categories, party limits |
| `starting-equipment.json` | Level-1 starting gear kits keyed by class id / build option |
| `equipment-stats-overrides.json` | Mundane PHB item stats for sheet sync |
| `import.json` | iws.mx JSONP import URLs and SQLite target |
| `catalog-counts.json` | Expected entry counts per category (import acceptance) |

Full system specification: [../PROJECT.md](../PROJECT.md) (Alter Ego).
