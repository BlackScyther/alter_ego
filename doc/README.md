# Project documentation (`doc/`)

This folder is the **living documentation** for agents and humans. Keep it in sync with the codebase.

## Core documents

| File | Purpose |
|------|---------|
| [project.md](project.md) | **Readable project description** (goals, users, architecture, status) |
| [requirements.md](requirements.md) | Functional & non-functional requirements (IDs, status) |
| [roadmap.md](roadmap.md) | Phases, completed plans, near-term backlog |
| [todos.md](todos.md) | Host / developer / agent checklists |
| [architecture.md](architecture.md) | Stack overview, API, data flows (why it stays simple) |
| [game-editor.md](game-editor.md) | GM encounters: rest mode, initiative, rewards |
| [files.md](files.md) | What each project file does and how it connects |
| [bugs.md](bugs.md) | Known bugs, limitations, and fix backlog |
| [tests.md](tests.md) | Test strategy, manual checks, and automation status |

## Deployment & desktop

| File | Purpose |
|------|---------|
| [deploy-vps.md](deploy-vps.md) | Default deploy target (`server.masche.at`, Coolify + Docker): Alter-Ego specifics on top of the `server-masche-deploy` skill |
| [deploy-online.md](deploy-online.md) | VPS: static app + Node API + SQLite |
| [deploy-hosting.md](deploy-hosting.md) | Webhosting: SSH/SFTP/Git checklists, fill-in profile, timeline |
| [mac-build-without-mac.md](mac-build-without-mac.md) | macOS `.dmg` via GitHub Actions (no Mac required) |

## Maintenance rule

After **every completed agent task** (feature, fix, or refactor), update:

1. **project.md** — if scope, features, architecture, or user-facing goals changed  
2. **requirements.md** — if requirement IDs or status changed  
3. **roadmap.md** / **todos.md** — if phases or backlog changed  
4. **files.md** — if files were added, removed, renamed, or their role changed  
5. **bugs.md** — if bugs were fixed, found, or deferred  
6. **tests.md** — if test steps or coverage changed  
7. **architecture.md** — if stack or API shape changed  

Also update the root **[PROMPT.md](../PROMPT.md)** when project goals, workflows, or agent conventions change.

Set **Last agent sync** below when you finish a doc pass.

**Last agent sync:** 2026-06-26 — Pre-staged the **braincell.online** custom domain for the live Coolify deployment: the Alter Ego app's Coolify domain list now holds `alter-ego.apps.masche.at`, `www.braincell.online`, and `braincell.online`, so Traefik already routes all three and will auto-issue Let's Encrypt certs for braincell the moment DNS points at the server (`37.120.178.213`). `PUBLIC_APP_URL` stays on the masche host until cutover. Updated `ops/env.example` (canonical www host + CORS note) and `doc/deploy-vps.md` (new "Custom domain: braincell.online" section with DNS records and the cutover checklist; Steps 4-5 reference the 3-host domain list). Earlier the same day: Made the user-level **`server-masche-deploy`** skill the **default deployment target** in the docs: `doc/deploy-vps.md` now opens with a "Default deployment target" banner that defers generic `server.masche.at` host ops (SSH, bootstrap → verify → harden order, Coolify deploy/redeploy, diagnosing, volume paths, day-2) to the skill and keeps only Alter-Ego specifics (volumes, env vars, compendium-DB upload, Dockerfile runtime gotcha), with a note that Steps 1-3 mirror the skill's "First-time host setup"; updated the deploy rows in `doc/project.md` and `doc/README.md` to name the skill as the default host workflow. The `.cursor/rules/ubuntu-server-setup.mdc` and `server-setup-ssh.mdc` rules already point to the skill. Earlier the same day: Documented server management + app deployment for the now-live setup: added a "Dockerfile runtime files" rule (runtime stage must ship `src/`, `metadata/`, `data/`), a day-2 management/diagnosis section (redeploy via Coolify, read deploy logs from `coolify-db` Postgres, reproduce crashes from the commit-tagged image, server clock is UTC, `masche` uses `sudo -n docker`), and the volume-path DB upload method in `.cursor/rules/ubuntu-server-setup.mdc` and `doc/deploy-vps.md`; refreshed `doc/files.md` (`.gitattributes`, Dockerfile copies) and `doc/project.md` (live URL). Earlier the same day: Alter Ego is **live** at `https://alter-ego.apps.masche.at` (Coolify + Docker on `server.masche.at`); `/api/health` returns `compendium: "sqlite"` and the container is healthy. Fixed two startup crashes in `Dockerfile` (commits on `main`): the runtime stage now copies `src/` (the API imports `src/encounter/rewards.js`, `src/character/actor-spawn.js`, etc. at runtime) and the whole `data/` tree (the `*-effect-overrides.json` config files; large SQLite DBs stay out via `.dockerignore` and arrive via volume). Uploaded the 76 MB compendium `alter_ego.db` into the `/data-public` persistent volume. Added `.gitattributes` to force LF on `*.sh`/`ops/**` so shell scripts run on Linux from a Windows checkout. Earlier the same day: SSH/sudo user is **`masche`** (already exists, sudo-enabled), so the bootstrap is run with `DEPLOY_USER=masche` instead of creating a `deploy` user; updated the user/host/path references in `.cursor/rules/ubuntu-server-setup.mdc`, `.cursor/rules/server-setup-ssh.mdc`, and `doc/deploy-vps.md` (bootstrap command, login check, `/home/masche` upload paths). Earlier the same day: app domain for the setup phase is a wildcard `*.apps.masche.at`, so Alter Ego deploys under **`alter-ego.apps.masche.at`** for now (set as the Coolify domain + `PUBLIC_APP_URL`; swap for a final domain later) — updated `.cursor/rules/ubuntu-server-setup.mdc`, `doc/deploy-vps.md` (Steps 4/5/7), and `ops/env.example`. Earlier the same day: recorded the VPS host **`server.masche.at`** in `.cursor/rules/ubuntu-server-setup.mdc` and `doc/deploy-vps.md` (use it for SSH/SCP and the Coolify dashboard; raw IP only as a DNS fallback). Earlier the same day: switched the planned VPS target to **Ubuntu 26.04 LTS** and added the new Cursor rule `.cursor/rules/ubuntu-server-setup.mdc` (globs `ops/**`, `Dockerfile`, `docker-compose.yml`, `doc/deploy-vps.md`, `doc/deploy-online.md`) covering the bootstrap → verify-key-login → harden-ssh order, the Coolify/single-container deploy, the `/data-public` + `/data-private` volumes, and required env (`TOKEN_PEPPER`, `PUBLIC_APP_URL`). Updated `ops/vps/bootstrap.sh` (OS check + header now say 26.04; `fail2ban` `[sshd]` jail set to `backend = systemd` because 26.04 ships without rsyslog/`auth.log`) and the 24.04 → 26.04 references in `doc/deploy-vps.md`, `doc/project.md`, and `doc/files.md`. Earlier: Added Cursor rule `.cursor/rules/server-setup-ssh.mdc` documenting the local SSH key (`~/.ssh/id_ed25519`, ed25519, empty passphrase), the gitignored `.env.deploy.local` config keys (`DEPLOY_SSH_HOST`, `DEPLOY_USER`, `DEPLOY_PATH`, `SSH_KEY_PATH`, `SSH_KEY_PASSPHRASE`, `DEPLOY_PUBLIC_URL`) used by `scripts/push-live.mjs`/`pull-live.mjs`/`pull-live-data.mjs`, and deploy commands (`npm run deploy:live`, `deploy:live:app`, `pull:live`). Generated the ed25519 key pair on this machine. Earlier: Made **subraces optional**: the race step's subrace picker now lists a **"None (play the base race)"** option first, so a race that has subraces can be played as the base race only (per player feedback). Implemented simply — "None" is a sentinel entry in the subrace list that confirms the base race id; no persisted flag. `validateRaceStep`/`raceSubraceComplete` (`race-selections.js`) treat any set `raceId` as complete (the wizard still blocks via "Select a race" while `raceId` is null), and the picker phase restore is simplified to "raceId set ⇒ confirmed". UI in `src/editor/steps/race-step.js` (`NO_SUBRACE_ENTRY` prepended in `renderSubraceOptions`, handled in `onPickSubrace`). New `tests/race-selections.test.mjs` cases; 324 tests pass. Earlier: Added a **per-power damage-type picker** for racial powers (Dragon Breath). The power's grant fold now shows two dropdowns — the attack ability (Strength/Constitution/Dexterity) and the damage type (acid/cold/fire/lightning/poison) — both required to finish the race step and both annotating the power name in the notes (e.g. `Dragon Breath (Dexterity, fire)`). `parsePowerAbilityOptions` (`src/character/power-ability-parse.js`) now also reads the `Special:` "choose ... as the ability score" phrasing, and a new `parsePowerDamageOptions` reads the "damage type: ..." list. New normalized table `power_damage_option(power_id, choice_group, damage_type)` (`tools/normalize/schema.sql` + `normalizePowerDamageOptions` in `normalize.mjs`) with read API `getPowerDamageOptions` on both compendium providers (table-guarded, runtime-parse fallback). Storage: `selections.racePowerDamageChoices` + `selections.racePowerChoiceReqs` (`model.js`); `setRacePowerDamageChoice`, reqs recording + notes annotation in `syncRaceNotesAndGrants`, and required-pick checks in `validateRaceStep` (`race-selections.js`). UI: `renderPowerDamageChoice` + grant-fold threading (`race-parse.js`), preview handler/wiring (`race-step.js`), the grants panel (`race-grants-panel.js`), and CSS. New `tests/power-options-parse.test.mjs` plus race-selections + normalize cases; 322 tests pass. Data caveat: the real Dragonborn/Dragon Breath entries live only in the gitignored DB, so parsers were validated against fixtures with the normalized table backstopping runtime parsing. Earlier: Added **monster-race lore folds**: races that carry little or no flavor in their own Race entry (e.g. Bugbear, defined primarily as a monster) now show supplemental **flavor-only** lore in the generator's race preview, pulled from the matching Monster compendium entry and rendered as a collapsed, expandable fold (statistics, power/attack blocks, tables, and stat-line paragraphs are stripped — prose only). New `extractMonsterFlavorFolds` in `src/character/race-parse.js` (conservative prose filter); resolution lives in `src/editor/steps/race-step.js` (`loadMonsterFluffEntries`), gated so it only supplements flavor-less races and matches the race Name to an **exact** monster Name (which excludes named individuals like "Billy the Squire" and variants like "Bugbear Strangler"), with a curated override map `metadata/race-monster-fluff.json` (loaded by the new `src/character/monster-fluff.js`: `monsterIds`/`monsterNames`/`disabled`). Preview-only (no sheet/stat changes, no new normalized table); folds append to the existing `renderFlavorFoldsBlock` via `opts.monsterFlavor` in `renderCombinedRacePreviewHtml`. New `tests/monster-fluff.test.mjs` (7 cases); 314 tests pass. Note: the real Bugbear/Monster entries live only in the gitignored compendium DB, so the extractor was validated against a representative statblock+lore fixture; the curated map backstops any entry the heuristic misses. Earlier: Added **subrace trait replacement**: when a subrace replaces a base-race trait or granted power/feat (e.g. a Draconian subrace's flight replacing a Dragonborn trait/power), the base item is now suppressed from the combined preview, notes, grant tiles, and sheet so only the subrace's version shows. Detection is hybrid: `parseRaceReplacements` in `src/character/race-parse.js` auto-parses "replaces … (trait|power|feat)" / "instead of …" phrasings from the subrace body, merged with a curated override map `metadata/race-replacements.json` (loaded by the new `src/character/race-replacements.js`). New normalized table `race_replacement(race_id, replaces_name, replaces_kind)` (`tools/normalize/schema.sql` + `normalize.mjs`, which also emits a `replacement-unmatched` warning when a trait target matches no base trait) with read API `getRaceReplacements` in `src/data/compendium.js` + `server/compendium.mjs` (table-guarded, runtime-parse fallback). Suppression: `resolveReplacedBaseItems` drives row/grant-fold filtering in `renderCombinedRacePreviewHtml`/`renderGrantFoldsBlock`; `collectRaceGrantIds` drops replaced base power/feat ids (by curated id or resolved name); `buildRaceNotesText`/`syncRaceNotesAndGrants` skip replaced base trait lines; `race-step.js` threads the grant-name map + replacements through preview and sync. Flavor stays in collapsed folds; subraces without their own Average Height/Size correctly inherit them from the base race. New `tests/race-replacements.test.mjs` (8 cases); 307 tests pass. Earlier: Added **structured racial traits, sheet application, and a racial-power ability choice**. New normalized tables (`tools/normalize/schema.sql` + `normalize.mjs`): `race_language`, `race_resistance`, `race_sense`, `race_trait`, `power_ability_option`, plus `race.speed_squares`. New parsers: structured race-trait parsers in `src/character/race-parse.js` (`parseRaceLanguages`/`parseRaceResistances`/`parseRaceSenses`/`parseRaceTraits`/`parseSpeedSquares`, `getRaceStructuredTraits` + formatters) and `src/character/power-ability-parse.js` (`parsePowerAbilityOptions` — detects an attack-line ability "or"-list). Read API: `getNormalizedRaceTraits`/`getPowerAbilityOptions` added to `src/data/compendium.js` and `server/compendium.mjs` (table-guarded, parse fallback). Generator: descriptive racial traits now render in one **collapsed-by-default "Racial Traits" fold** (`renderRaceTraitsBlock`), with the ability-score choice kept visible above it; racial powers that offer an ability-score choice show a dropdown (in the grant fold and in `steps/race-grants-panel.js`) and store the pick per character at `selections.racePowerAbilityChoices` (`model.js` + `setRacePowerAbilityChoice`). Sheet: `applyRaceTraitsToSheet`/`clearRaceTraitsFromSheet` (`race-selections.js`, wired into `syncRaceNotesAndGrants`) write Languages, Resistances, Special Senses, and Speed to their sheet/mirror fields with **manual-vs-derived precedence** (race is the authoritative baseline; user edits are kept as a manual addendum and survive re-sync). 299 tests pass. Earlier: Corrected the misspelled project slug `alter-eger` / `alter_eger` / `Alter_Eger` (ER) to `alter-ego` / `alter_ego` / `Alter_Ego` (O) across code, scripts, config, Docker, env, and docs. The compendium database file is now `data/alter_ego.db` (was `alter_eger.db`); all runtime, build (`post-build-app.mjs`), deploy (`push-live.mjs`), normalize, and audit-script references were updated, along with `package.json` `name`, the dev DB (`alter_ego.dev.db`), PDF output names, the auth dev-pepper default, the test temp-DB prefix, and deploy-path placeholders. Added a one-time server migration note in `doc/deploy-vps.md` (remove the stale `alter_eger.db` from `/data-public`). The already-correct `alter-ego-*` Docker container/volume and asset names were left unchanged. 299 tests pass. Earlier: Added **single-container VPS hosting** so the whole app (static frontend + Express API) runs from one Node process at the same origin, ready for a fresh Ubuntu 24.04 LTS server. `server/index.mjs` now optionally serves the built frontend and a public data dir when `STATIC_DIR`/`PUBLIC_DATA_DIR` are set (and the rate limiter is scoped to `/api` so static assets are never throttled). `server/routes/campaigns.mjs` invite URLs use a configurable `PUBLIC_JOIN_PATH` (default `/src/join/index.html`; root-served prod uses `/join/index.html`). New deploy assets: root `Dockerfile` (multi-stage build, prunes dev deps, ships `dist/app` + server; the ~77 MB compendium DB is NOT baked in — provided via a `/data-public` volume at runtime), `.dockerignore`, `docker-compose.yml` (binds `127.0.0.1:3000`, two volumes `/data-public` public + `/data-private` for `campaigns.db`), `ops/env.example` (all env incl. `TOKEN_PEPPER`, `PUBLIC_APP_URL`, DB paths; copy to gitignored `ops/server.env`), `ops/vps/bootstrap.sh` (Ubuntu 24.04 hardening: non-root sudo user + key, unattended-upgrades, swap, `ufw`, `fail2ban`, installs Coolify which installs Docker — does NOT disable SSH passwords to avoid lockout), and `ops/vps/harden-ssh.sh` (key-only SSH, run after confirming key login). New guide `doc/deploy-vps.md` (accessible, browser-first via Coolify: bootstrap → harden → create admin → DNS → deploy this repo's Dockerfile with two persistent volumes + `TOKEN_PEPPER`/`PUBLIC_APP_URL` → upload `alter_ego.db` into `/data-public` → verify `/api/health` → backups → hosting more projects). Earlier: Fixed the deployed character generator's dead **"Create new character"** button (B-031). The editor's `init()` (`src/editor/editor.js`) attached all control handlers only after several un-guarded `await` fetches, so a missing supporting file on the host (e.g. `metadata/editor.json` when only part of `dist/app` was uploaded) made `init()` throw before binding any buttons, leaving them silently inert (it worked for the owner via cache/dev). Refactored: control wiring moved into `wireControls()` and run **before** any network load (gate shown immediately); `loadEditorMeta` validates the response and throws a clear message; the load sequence is wrapped in try/catch surfacing a visible `#generator-init-error` banner (`src/editor/index.html`); `init()` has a `.catch`. Hardened `doc/deploy-hosting.md` (upload the entire `dist/app/` tree incl. `metadata/`+`data/`; verify those URLs resolve; incognito smoke test) and added a troubleshooting row. Not an auth/token issue — the generator gate requires no token. Earlier: Added a **Feedback and tracking** system. A shared **Feedback** menu item (player hub, launcher, home hub, GM console, character generator sidebar) opens a categorized submission form (`src/feedback/`: problem/stuck/wrong/missing/feature/different + optional area/severity/contact, auto app version/role) that POSTs to a new public, rate-limited `/api/feedback` endpoint (`server/routes/feedback.mjs` + `server/validate-feedback.mjs`), stored in a new SQLite `feedback` table (`server/db.mjs`: `insertFeedback`/`listFeedback`/`getFeedbackStats`/`updateFeedbackStatus`/`deleteFeedback`). A GM-only statistics page (`src/gm/feedback/`) reads `GET /api/feedback` + `/stats` (GM token) to show totals, last-30-days, by category/area/status, and most-recurring category+area, and lets the GM filter, change status (open/triaged/resolved/wontfix), and delete entries. Client in `src/api/feedback-api.js`; `'feedback'` added to the Vite trailing-slash segments. New `tests/feedback-api.test.mjs` (public submit + validation, GM-only list/stats/PATCH/DELETE, aggregation); 299 tests pass. Earlier: Extended the magic-item enhancement to **implements** and to the **printable power cards**. Implements now resolve to a `kind: 'implement'` stat in `src/character/equipment-stats.js` and are flagged enhanceable, so the Equipment step's Magic bonus selector also appears on equipped implements. `syncEquipmentToSheet` (`src/character/equipment-sheet-sync.js`) collects the equipped implement's enhancement, shows the higher of the weapon and implement bonus on the Melee/Ranged attack lines, and stores per-source values (`weapon-melee-enh`/`weapon-ranged-enh`/`implement-enh`) for the cards. `src/character/power-card-values.js` now folds those into `attackTotalFor`/`damageModFor` (weapon powers use the weapon's bonus per line, implement powers the implement's), and `src/print/cards.js` detects implement vs weapon powers to apply the right source (weapon enhancement now also reaches weapon power cards). New tests in `tests/equipment-sheet-sync.test.mjs` (implement enh + max(weapon, implement)) and `tests/power-card-values.test.mjs` (weapon/implement card enhancement); 291 tests pass. Earlier: Generalized magic-item enhancement so any equipped **base weapon or armor can be made magic (+1/+2/+3)**, extending the prior Amulet-of-Protection-only mechanism. Equipment instances now carry a per-item `enhancement` (`setEquipmentItemEnhancement` in `src/character/equipment-selections.js`, clamped 1–3). `src/character/equipment-stats.js` gained config-driven helpers (`getMagicTiers`/`magicTierForBonus`/`isEnhanceableEntry`/`magicDisplayName`/`baseCostGpFromEntry`/`magicTotalCostGp`) reading new GM-editable `metadata/magic-equipment-tiers.json` (bonus → level + `magicCostGp`; defaults +1@L3, +2@L11, +3@L22; total price = base item cost + tier price). `syncEquipmentToSheet` (`src/character/equipment-sheet-sync.js`) now applies a magic weapon's bonus to melee/ranged `atk`+`dmg` `enh` and a magic armor's bonus to AC `enh` (resetting all four defense `enh` parts plus the new attack/damage enh keys each sync). The Equipment step (`src/editor/steps/equipment-step.js`) shows an inline **Magic bonus** `<select>` (None/+1/+2/+3) on equipped base weapons/armor, displaying the derived name ("+2 Chainmail"), item level, and price (styled in `src/editor/editor.css`); equipped/inventory rows show the magic name. AC `enh` is now equipment-managed/readonly like Fort/Ref/Will (`src/editor/sheet-mirror-fields.js`). Added magic weapon/armor + tier-lookup cases to `tests/equipment-sheet-sync.test.mjs` (286 tests pass). Earlier: Removed **Universal actions** from the character-collection "Print cards" flow (they now live on the shared Resources page and would be duplicated otherwise); the collection panel still displays them. Dropped the `universal` key from `CATEGORY_LABELS` in `src/editor/print-cards-dialog.js` and from `PRINT_CARD_SECTION_KEYS` in `src/editor/editor.js`, and `src/print/cards.js` no longer loads `loadUniversalActionsMeta` or renders a Universal actions section (prints only the character's own Powers/Feats/Rituals). Earlier: Ensured the character sheet is reachable both ways: `openSheet` (`src/editor/editor.js`) opens the populated sheet in a new tab and now falls back to same-tab navigation if the browser blocks the popup (dropped the `noopener` flag so a blocked popup is detectable), and the sheet toolbar (`src/sheet/index.html`) leads with a prominent **← Character generator** back link (`#back-to-editor`, `.toolbar-back` accent in `src/sheet.css`) that `src/app.js` keeps in player mode (`?mode=player`) when applicable. Also removed the now-dead `#level-reference` print rule from `src/sheet.css`. Earlier: Added a shared **Player resources** page and removed the static level table from the printable character sheet. The character sheet (`src/sheet/index.html`) no longer appends the "Values by Level (1–30)" reference page (it never changes per character), so printing the sheet now emits just the character page; its toolbar "Level 1–30 reference" link now points to the new Resources page (`renderLevelTable` in `src/app.js` was already null-guarded, so no JS change). New character-independent reference page `src/resources/` (`index.html` + `resources.js` + `print-resources-dialog.js` + `resources.css`), linked from the Player hub (`src/player/index.html`) and GM console (`src/gm/index.html`): it renders the Values by Level (1–30) table (`buildLevelTable`), a formula key, and the Universal actions every character gets (hydrated from `metadata/universal-actions.json` via `loadUniversalActionsMeta` + `compendium.getEntry` + `condenseRuleHtml`). A **Print** button opens a pop-up section-picker dialog (modeled on `print-cards-dialog.js`, remembered in `sessionStorage`) and prints only the chosen sections by toggling `.no-print`. Added `'resources'` to the Vite trailing-slash redirect segments; the multipage build auto-discovers the new page. `src/levels.html` is retained for `pdf:levels`. Earlier: Made **Player Name** optional in the character generator: it is informational only (never used in any calculation), so the `basics` step in `src/character/validateStep` (`src/character/model.js`) no longer pushes "Player name is required." and never blocks step navigation. Character name stays required (the functional identifier for saving, file names, lists, and summaries); level keeps its 1–30 check. Earlier: Moved the "open character sheet" action into the wizard character-sheet mirror as a top-right **Print sheet** button (like the Collection's Print cards). `initSheetMirror` (`src/editor/sheet-mirror.js`) now takes an `onPrintSheet` callback and renders a `.sheet-mirror-print` button in the mirror header (styled in `src/editor/editor.css`); `openSheet` (`src/editor/editor.js`) is wired to it and now opens the populated scalable sheet `src/sheet/index.html?from=editor` in a **new tab** (`window.open`) instead of navigating the current tab, keeping the wizard open. Removed the now-redundant `#nav-sheet` left-nav link and the bottom `#btn-sheet` "Open character sheet" button (and its listener) from `src/editor/index.html`/`editor.js`. The sheet remains populated via the existing `characterToSheetPayload` → `dnd4e.sheetPayload` → `applyEditorPayload` flow. Verified every editable mirror field maps onto a sheet input; the one gap was racial powers — added a dedicated **Racial Powers** textarea (`racial-powers`) to `src/sheet/index.html` so the existing `racial-powers` payload key prints (no bridge/mirror change). 281 tests pass. Earlier: Simplified the printable rule cards to a single, readable A4 layout: removed the 9/page (3-column) density mode and its toolbar toggle, so cards always print large at about **four per page**. Cards use a **two-column grid** in `src/print/cards.css` with a `--card-h` per-card minimum height (not a rigid fixed height), so at most four cards print per page yet a card with long rule text **grows past the minimum instead of being clipped** and moves to the next page when its row no longer fits (important because printed pages can't scroll). The on-screen preview is not paginated; the four-per-page result only shows in Print / Save as PDF. `src/print/cards.css` also has larger card fonts; `src/print/cards.js` drops the density-toggle logic and the `print.cards.density` key; `src/print/cards.html` drops the density buttons. Earlier: Added category selection and an A4 layout to the printable rule cards. Clicking **Print cards** opens a native `<dialog>` (`src/editor/print-cards-dialog.js`, styled in `editor.css`) listing only the categories the character has (Powers/Universal actions/Feats/Rituals), remembering the last choice in `sessionStorage`; `openPrintCards` in `src/editor/editor.js` is async, derives available categories from the rendered collection sections, and hands off `{ character, categories }` via the `editor.printCards` localStorage key. Earlier: Personalized the printable power cards with this character's attack/damage numbers. New `src/character/power-card-values.js` (`buildPowerCardContext`, `attackTotalFor`, `damageModFor`, `abilityKeyFromName`, `formatSigned`) computes the full to-hit (half level + ability modifier + equipped weapon proficiency synced to `sheet.extraFields['melee-atk-prof']`/`['ranged-atk-prof']`, + any inline power bonus; weapon enhancement/feat bonuses not modeled yet, default 0) and the damage modifier (ability modifier, no half level). `src/print/cards.js` now annotates power/universal-action tiles: Attack ability terms gain the to-hit (e.g. "Strength +9 vs. AC", folding inline "+ N"), and "<ability> modifier" damage terms gain the value (e.g. "Strength modifier (+5)"), both wrapped in `.rule-card-calc`; weapon vs implement and melee vs ranged are detected from the keyword/action text. Added `tests/power-card-values.test.mjs` (7 cases, wired into `npm test`). Earlier: Added printable rule cards for the character collection. A new **Print cards** button in the wizard's character-collection toolbar (`renderExpandCollapseAll` in `src/editor/steps/character-collection-panel.js`, `.character-collection-print` in `editor.css`) serializes the working character to the `editor.printCards.character` localStorage handoff and opens `src/print/cards.html` in a new tab (`openPrintCards` in `src/editor/editor.js`). The cards page (`src/print/cards.{html,js,css}`) reuses `collectAllCollectionItems` to gather the character's powers, universal actions, feats, and rituals, then renders one tile each via `condenseRuleHtml(entry)` (new `src/ui/condense-rule-text.js`), which strips `flavor`/`flavortext`/`publishedIn` nodes and the duplicate power/entry title `<h1>` while keeping the mechanical `powerstat` lines, feat benefit, and ritual stat block. Tiles get an at-will/encounter/daily border accent from the power Type and are print-optimized (`@media print`, `page-break-inside: avoid`); falls back to the active stored character when opened directly. Earlier: Made every character-collection category collapsible and collapsed by default to save vertical space at high levels. `renderSection` in `src/editor/steps/character-collection-panel.js` now builds a `<details>/<summary>` (reusing the `.character-collection-universal` chevron styling) instead of a static `<section>/<h4>`, so Powers, Feats, Rituals, and Universal actions all start folded and can be opened individually. Open/closed state persists per category in `sessionStorage` (key prefix `editor.characterCollection.section.`). Added an "Expand all / Collapse all" toggle (`renderExpandCollapseAll`, `.character-collection-toolbar`/`.character-collection-expand-all` in `editor.css`) above the sections to reveal everything at once. Earlier: Added per-level item tiers and GM source-date integration. ETL: new `item_level(item_id, tier, level, cost_gp, enhancement)` table + `parseItemTiers` in `normalize.mjs` splits level-scaled items (e.g. Amulet of Protection's six +1..+6 versions) from their compound `Level`/`Cost` fields into one row per tier (drops the "1+/360+ gp" summary token, zips/sorts the scrambled level/cost pairs, handles thousands-comma summaries) and now sets the parent `item.cost_gp` to the lowest tier instead of null; tier-mismatch warnings dropped from 1085 to 0. GM source dates: writable `metadata/source-books.json` is exposed via `GET/PUT /api/source-books` (`server/source-books.mjs` + `server/routes/source-books.mjs`, merged with all normalizer-discovered codes via `listSourceBookCodes()`), with `src/api/source-books-api.js` (DB fallback to `compendium.getSourceBookDates()`). `picker-source-combo.js` gained an opt-in "released on/before" date filter (`resolveSourceBooksByDate`) wired into the Workshop duplicate picker, plus a Workshop "Source dates" editing dialog (`workshop.js`/`index.html`/`workshop.css`). New normalize tests for the amulet tiers + single-level items (274 pass). Earlier: Started the compendium normalization: added a derived normalized SQLite schema and ETL (`tools/normalize/normalize.mjs` + `schema.sql`, `npm run normalize` / `normalize:stub`, wired into `build:app` via `--if-exists`) that reuses the existing parsers and writes structured tables into the same `data/alter_ego.db` (source_books with release dates, race/subrace split, classes incl. `is_hybrid`/`hybrid_parent_class_id` + proficiencies, powers with exact `power_type`, item/armor/weapon stats, backgrounds) plus a `normalize_warnings` data-quality report. Added normalized-first read methods with parse fallback in `src/data/compendium.js` and `server/compendium.mjs` (power filtering now uses the normalized `power` table when present; `syncEquipmentToSheet` prefers normalized item stats); `race-subraces.js` is hydratable from the `race_subrace` table at editor bootstrap. Implemented the PH3 hybrid merge in `src/character/hybrid-merge.js` (HP/surge math, skills, armor/shield intersection + weapon/implement union, `hybridPairAllowed`, `hybridPowerCoverage`) and enforced "not two subclasses of the same class" in the class step (B-024 partially addressed; live sheet-sync wiring remains). Added `metadata/source-books.json` (date-ready) and `doc/compendium-schema.md`. Then wired the B-024 hybrid merge into the live sheet: `applyHybridClassStats` (in `class-selections.js`, called from the class step) writes the PH3 merged HP, HP/level, surges, and speed to the sheet and persists `selections.hybridMerged`, and `computeMaxHp`/`computeLevel1MaxHp` prefer those values when hybrid mode is on (non-hybrid characters unaffected; falls back when the normalized DB is absent). Also derived `item.slot` (head/neck/arms/hands/waist/feet/ring) from each item body's "<X> Slot" line — the source `Type` is empty for most wondrous items — so amulets, cloaks, periapts, etc. now resolve to the correct body slot authoritatively (`getEntry` attaches it; `getEligibleSlotsForEntry` prefers it with a name-heuristic fallback). Fixes the long-standing "Amulet of Protection had no type/slot" bug (B-029). New tests: `normalize`, `hybrid-merge`, `subrace-hydrate`, `hybrid-sheet`, `equipment-slot` (271 pass). Remaining for B-024: surface the merged trained-skill pool and proficiencies in the skill/equipment UI and enforce `hybridPowerCoverage` in the power step. Earlier: Documented the authoritative D&D 4e hybrid-character rules (PH3:134-135) for the deferred B-024 merge: added a **Hybrid character rules** reference table to `doc/bugs.md` (two classes/subclasses, not two subclasses of the same class; one power of each type from both before a second; armor proficiency intersection, weapon/implement union; HP = average of starting HP rounded down + CON score; HP/level = sum of half each class's per-level HP; surges = average + CON modifier; combined class skills, trained in any three; combined trait subsets; Hybrid Talent feat). B-024 now links to it and notes the current name-prefix detection does not yet enforce the "not two subclasses of the same class" constraint. Earlier: Added level-scaled defense enhancement items (Amulet of Protection): equipping one now adds its enhancement bonus to Fortitude, Reflex, and Will. `src/character/equipment-stats.js` gained `getDefenseEnhancementInfo` (detects the item by name → which defenses), `enhancementFromLevel` (+1…+6, one step per five levels), and `levelFromEntry`; equipment items carry an optional per-instance `level` (`setEquipmentItemLevel` in `src/character/equipment-selections.js`); `syncEquipmentToSheet` resets and re-derives Fort/Ref/Will `enh` from equipped enhancement items; the equipment step shows an accessible inline **Item level** number field on such equipped items (chosen over a modal for NVDA); the Fort/Ref/Will `ENH` mirror fields are now readonly (equipment-managed). Earlier: Fixed defense totals omitting the ability-score modifier (B-026): FORT/REF/WILL/AC in the wizard sheet mirror previously summed only `10 + ½ level + class + …` and never added the ability bonus (a level-3 character showed FORT 13 instead of 18). Added `defenseAbilityMod(scores, defense)` in `src/formulas.js` (higher of the two relevant ability mods: FORT max(STR,CON), REF/AC max(DEX,INT), WILL max(WIS,CHA)); `buildMirrorPayload` now derives each defense's ability component (heavy armor suppresses the AC ability bonus; `syncEquipmentToSheet` persists `sheet.armorIsHeavy`), and the `${def}-abil` mirror fields are readonly. Earlier: Made the wizard sheet-mirror panel taller and user-resizable: `.sheet-mirror-body` (`src/editor/editor.css`) now uses `height: max(40vh, 220px)` (a pixel floor so it stays usable at high browser zoom, where `40vh` previously collapsed the tab content to a few pixels), `max-height: 85vh`, and `resize: vertical` so the user can drag it taller; the floor uses `max()` instead of `min-height` so it never fights the `max-height: 0` collapse animation, and the collapsed state also sets `resize: none`. Earlier: Fixed AC Total ignoring equipped armor in the wizard sheet mirror (B-025): `refreshBonusesFromSelections` (`src/editor/editor.js`) now re-runs `syncEquipmentToSheet` on every bonus refresh so loaded/switched/imported characters reflect equipped gear (previously armor only synced on the Equipment step), and `getEquipmentStats` (`src/character/equipment-stats.js`) falls back to a PHB armor table keyed by Name/Type (Plate +8, Scale +7, Chainmail +6, Hide +3, Leather +2, Cloth +0) when an armor has no stats override and no parseable inline AC bonus. Earlier: Highlighted the final/summed result fields in the wizard character-sheet mirror so they stand out from their contributing parts (e.g. AC Total vs the AC — ABIL/CLASS/FEAT components). Added a `total` flag in `src/editor/sheet-mirror-fields.js` on the summed fields (AC/FORT/REF/WILL Total, Initiative, Max HP, melee/ranged Attack & Damage Bonus, Passive Insight/Perception, Speed, Action Points); `renderFieldHtml` in `src/editor/sheet-mirror.js` emits a `sheet-mirror-field--total` class, and `src/editor/editor.css` frames those inputs with a 2px accent border, accent-tinted background, glow, and accent-coloured bold label (overriding the dimmed readonly style). Earlier: Added automatic version bumping on commit: a tracked `.githooks/pre-commit` hook runs `scripts/bump-version.mjs` to increment the `package.json` patch version (e.g. `1.2.4` → `1.2.5`) and stages `package.json` before each commit. Enabled locally via `git config core.hooksPath .githooks`; skip a single commit with `ALTER_EGO_SKIP_BUMP=1`. Earlier: Added a **Hybrid class** filter and dual picker to the Class step (UI/storage only). A checkbox (off by default) hides hybrid classes from the normal list; when on, only hybrid classes (`Name` starts with "Hybrid ") are listed and a second class picker appears, sharing the one source filter. Both pickers are stacked full-width and pre-rendered (second toggled via `hidden`) so revealing/using the second never resizes the first. Selections persist as `selections.classHybrid` and `selections.hybridClassIds` while `selections.classId` stays the primary class; merging two classes into HP/skills/powers/sheet is deferred (B-024). Files: `src/editor/steps/class-step.js`, `src/character/class-selections.js`, `src/editor/editor.css`, `metadata/editor.json`. Earlier: Fixed Max HP not scaling with level/CON (B-023): Max HP was frozen at the level-1 value because it was stored once under a `level === 1` guard and echoed by `buildMirrorPayload`. Added `computeMaxHp()`/`getClassHpPerLevel()` in `src/character/hp.js` (`classBase + CON/substitute + (level − 1) × hpPerLevel`); `syncClassTraitsToSheet` (`src/character/class-selections.js`) now persists `hp.classBase`/`hp.perLevel` and recomputes for the current level; `buildMirrorPayload` (`src/editor/sheet-mirror.js`) derives Max HP live; the mirror `max-hp` field is readonly (derived). Earlier: Added 4e ability score increases on level up (B-022): the Abilities step now shows ASI controls — at levels 4/8/14/18/24/28 the player raises two different abilities by +1 each via per-level dropdowns, and at levels 11/21 all six abilities gain +1 automatically. Choices are stored in `character.abilities.levelIncreases` and folded into `abilityScoreBreakdown` (`src/character/tutor.js`) as a stacking "Level" component, so totals stack across levels and may exceed 18. `enterRetrainingFlow` (`src/editor/editor.js`) routes the player to the Abilities step whenever an ASI choice is pending, and `validateStep('abilities')` (`src/character/model.js`) blocks progress until each pending pair-level has two distinct abilities chosen. Earlier: Fixed Level up stuck at level 2 (B-021): `enterRetrainingFlow` (`src/editor/editor.js`) no longer overwrites `identity.totalXp` with the new level's threshold on level-up; it now increments the level only and preserves accumulated XP, so a character with enough XP (e.g. 4500) can level up repeatedly (to level 4) instead of resetting to 1000 after reaching level 2. Earlier: Race build choices (`race-step.js`) with more than 6 options now render as a compact combo `<select>` (`.race-build-combo`) with a single open-in-compendium ↗ link that tracks the current selection, instead of a long radio list (e.g. the 13 Genasi elemental manifestations). Smaller option sets keep radio buttons; `choice-guide.js` now treats a non-empty build combo as a completed choice. Earlier: Added an always-visible round GitHub link (`.nav-github`) at the bottom of the GM console sidebar (GM home, Workshop, Campaigns, Encounters), the character editor's left sidebar (`editor/index.html`, the player-facing left nav), and the Player hub, pointing to the public repo `https://github.com/BlackScyther/alter_ego` (opens in a new tab, screen-reader labeled). Earlier: Fixed editor horizontal overflow at high zoom/narrow viewports (`.editor-nav`/`.editor-main` now use `min-width: 0`), so right-side controls like the equipment slot/inventory "open in compendium" ↗ link and ✕ clear button stay on-screen (B-020). Earlier: character collection panel now starts collapsed by default on first wizard open (state persists per session in `sessionStorage`, key `editor.characterCollection.collapsed`). Earlier: added Warlord (Marshal) starting-equipment kits (`class8` + name entry, all five build options) so the Equipment step "Recommended equipment for this class" button now enables for Warlords instead of staying disabled (B-019). Earlier: Attributes step gained a "Recommended ability scores for this class" button that auto-distributes the 22-point budget toward the class's Key Abilities; `autoPointBuy` moved to `model.js` and shared with quick-build; race ability "choose any one" bonus renders a compact combo `<select>`.
